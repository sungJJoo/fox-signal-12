# 설계 맥락 노트 — 에이전트와 함께해요 2 · 지하 12층

이 문서는 프로그램 전체의 **단일 기준값**이다. 좌표·변수·명령어를 바꿀 때는 반드시 여기부터 고친다.

---

## 1. 이 프로그램의 위치

아이들이 폭스 AI 연구소에서 거치는 세 번째 프로그램이다.

```
마을을 지켜라  →  에이전트와 함께해요  →  에이전트와 함께해요 2 (여기)
좌표평면 이론      명령·반복·조건            좌표 계산·변수·자동화
직업별 장문 코드   에이전트 조작 기초         에이전트 원격 관제
```

### 아이들이 이미 하고 오는 것 — 가르치지 않는다

| 출처 | 내용 |
|---|---|
| 마을을 지켜라 OT | 좌표 개념, 좌표평면 (`좌표이론.pdf` · `좌표평면2.pdf`) |
| 마을을 지켜라 | 직업별(광부·농부·전사) 장문 코드, 방어 시스템 설계 |
| 에이전트와 함께해요 | `move` `turn` `place` `destroy` `collectAll` `setSlot` `teleportToPlayer` |
| 에이전트와 함께해요 | `for` 반복, `if` + `detect`, 중첩 반복, 통합 루틴(`base`) |

**좌표 보기 켜는 법, 채팅창 여는 법, MakeCode 여는 법은 안내하지 않는다.**
1차시부터 위 목록을 전제로 바로 시작한다.

### 1탄의 한계와 이번 버전의 해결

| 1탄의 한계 | 2탄의 해결 | 도입 차시 |
|---|---|---|
| 숫자를 바꾸려면 코드를 열어야 했다 | 채팅으로 숫자를 넘긴다 (`dive 5`) | 2 |
| 에이전트가 항상 내 옆에서만 시작했다 | 절대 좌표로 어디든 보낸다 | 1 |
| 반복의 `i` 가 횟수 세기용일 뿐이었다 | `i` 를 좌표 계산의 재료로 쓴다 | 6 |
| 값을 기억할 방법이 없었다 | 변수로 여러 명령이 값을 공유한다 | 4 |
| 결과가 눈으로만 남았다 | 개수·합계를 숫자로 보고한다 | 7 · 9 |
| 아이가 볼 교재가 없었다 | 웹 교재에 층별 계산·관찰·비교표 | 전 차시 |

---

## 2. 세계관

> 폭스 연구소 지하에는 30년 전 봉인된 시설이 있다.
> 3주 전부터 그 아래에서 신호가 올라온다. 신호는 언제나 숫자 세 개만 반복한다.
> 사람은 내려갈 수 없다. 에이전트도 내 옆에 있으면 못 내려간다.
> 옆이 아니라 **좌표로** 보내야 한다.

- 아이의 역할: **관제실 오퍼레이터**. 직접 내려가지 않는다.
- 12층에 있는 것: **30년 전 봉인된 최초의 에이전트**. 신호는 좌표를 보내며 "여기 있다"고 부른 것이었다.
- 1탄과의 연결: 최초의 에이전트는 "명령을 코드에 하나하나 박아 넣던 시절"의 에이전트다. 1탄 방식 그 자체가 결말의 상징이 된다.

---

## 3. 좌표 규칙 (전 차시 관통)

### 층 → Y 변환

```
Y = 62 - (층 × 4)
```

| 층 | Y | 층 | Y |
|---|---|---|---|
| 1 | 58 | 7 | 34 |
| 2 | 54 | 8 | 30 |
| 3 | 50 | 9 | 26 |
| 4 | 46 | 10 | 22 |
| 5 | 42 | 11 | 18 |
| 6 | 38 | 12 | 14 |

5차시에서 아이가 이 식을 스스로 찾아내 코드로 옮긴다. 6·9·11차시에서 계속 재사용된다.

### 수직 갱도 (관제 통로)

- X = **-12**, Z = **8** 고정
- 내부 3×3 (X -13~-11, Z 7~9), Y 14 ~ 79

### 층별 신호 좌표

Y는 규칙으로 계산되지만 **X·Z는 층마다 다르다.**

| 층 | X | Y | Z | 층 상황 |
|---|---|---|---|---|
| 1 | -22 | 58 | 18 | 갱도에서 떨어진 첫 지점 |
| 2 | -4 | 54 | -6 | Z가 음수로 |
| 3 | -26 | 50 | 2 | 반대쪽 끝 |
| 4 | 0 | 46 | 20 | 모서리 |
| 5 | -12 | 42 | -10 | 갱도 남쪽 |
| 6 | 0 | 38 | 16 | 조명이 꺼져 있다 |
| 7 | -24 | 34 | 20 | 통로가 무너져 있다 |
| 8 | -6 | 30 | -10 | 물이 찼다 |
| 9 | -25 | 26 | 7 | 바닥이 없다. 신호 지점만 섬으로 남는다 |
| 10 | 0 | 22 | 22 | 갈림길이 많다 |
| 11 | -18 | 18 | -12 | 신호가 강해진다 |
| 12 | -12 | 14 | 8 | 갱도 바로 아래. 계속 발밑에 있었다 |

### 층 홀 범위 (월드 빌드용)

- 내부 공간: X **-30 ~ 2**, Z **-14 ~ 24**, Y `층Y` ~ `층Y + 2`
- 바닥: `층Y - 1`
- 돌 외벽: X -31 ~ 3, Z -15 ~ 25, Y `층Y - 2` ~ `층Y + 3`

층 간격 4 = 바닥 1칸 + 공간 3칸.
**층 높이가 3칸뿐이라는 제약이 6차시의 학습 장치다.** `i` 를 Y에 넣으면 금방 천장에 막힌다.

### 층 공통 구조

모든 층이 같은 뼈대를 쓴다. 아이가 어느 층에 가도 방향을 잡을 수 있어야 하기 때문이다.

| 구역 | 범위 | 역할 |
|---|---|---|
| 관제 홀 | X -16~-8, Z 4~12 | 갱도를 감싼 방. 사방에 문 하나씩 (-16,8) (-8,8) (-12,4) (-12,12) |
| 층 번호 점 | X -17~, Z 14 | 발광석을 층 수만큼. 세어보면 몇 층인지 안다 |
| 천장 조명 | 8칸 격자 | 6층만 없다 |
| 외곽 배관 | 테두리 | 방이 아니라 시설로 보이게 하는 장식 |
| 신호 지점 | 층별 | 강철 3×3 단상 + 붉은 코어. **가운데 Y칸은 비운다** |

**신호 표식 규칙** — 에이전트가 서는 높이(Y)에는 아무것도 놓지 않는다. 바닥(Y−1)과 천장(Y+2)에만 놓는다. 그래야 그 좌표로 투입할 수 있다. 12층 격납고 단상도 같은 규칙을 따른다.

### 미션 동선 — 여기에 구조물을 놓으면 코드가 막힌다

| 층 | 비워둘 곳 |
|---|---|
| 6 | Z=16 의 X −28~−13 · X=−12 의 Z −12~4 · 대각선 (−28,−12)~(−19,−3) |
| 7 | Z=20 의 X −24~−12 **← 여기만 일부러 막는다** |
| 8 | X −10~−2, Z −8~2 (물) |
| 9 | X −30~−20, Z 2~12 (바닥 없음) |
| 10 | Z=8 의 X −12~0 · X=0 의 Z 8~22 |
| 11 | Z=8 의 X −12~−30 · 네 귀퉁이 (−26,−10) (−2,−10) (−2,20) (−26,20) |
| 12 | Z=8 의 X −12~−28 |

`world/build.js` 를 고칠 때는 이 표를 먼저 본다.

---

## 4. 커리큘럼 — 새로 배우는 것만

| 차시 | 새로 배우는 것 | 왜 그 자리인가 |
|---|---|---|
| 1 | `world()` + `agent.teleport` | 지하는 내가 못 가는 곳이다 |
| 2 | `onChat` 의 `num1` | 층마다 깊이가 달라 코드를 매번 못 고친다 |
| 3 | `num1` `num2` `num3` | 좌표는 숫자 세 개다 |
| 4 | `let` 변수 | 매개변수는 명령이 끝나면 사라진다 |
| 5 ★ | 변수끼리 계산 (`62 - 층 * 4`) | 열두 층 좌표를 다 외울 수 없다 |
| 6 ★ | 반복 변수를 좌표에 | 정확한 간격으로 놓아야 한다 |
| 7 | 조건 안에서 변수 키우기 | 관제실은 숫자로 보고받는다 |
| 8 | `i` 와 `j` 를 다른 축에 | 선이 아니라 면이 필요하다 |
| 9 | 누적 `sum = sum + x` | 재료를 미리 계산해야 한다 |
| 10 | 좌표의 차와 부호 | 세지 말고 계산하라 |
| 11 ★ | 전부 합치기 | 스물두 개 작업을 명령 두 개로 |
| 12 | 통합과 설계 | 내가 쓸 도구를 만든다 |

★ = 이해가 갈리는 차시. 시간이 모자라면 다른 활동을 줄여서라도 핵심 활동을 지킨다.

### 빼는 것과 이유

- **함수** — `onChat` 자체가 이미 함수 역할을 한다. 개념이 겹쳐 혼동을 만든다.
- **배열** — 인덱스가 좌표(X·Y·Z)와 충돌해 좌표 학습을 흐린다.
- **나머지 연산 `%`** — 초등 고학년에게 무리. 줄무늬는 반복 안에 두 칸을 넣어 해결한다.

### 코드 분량 기준

차시당 명령 **4~5개**, 실제 코드 **56~101줄** (주석 제외).
마지막 비필수 명령은 지도안에 **선택**으로 표시했다. 진도가 밀리면 그것부터 버린다.

---

## 5. MakeCode 블록 변환 제약 (중요)

코드는 **MakeCode JS 탭에 붙여넣어 블록으로 변환**해서 아이에게 보여준다.
따라서 static TypeScript 서브셋만 쓴다.

**쓴다**
`let` · `for (let i = 0; i < n; i++)` · `if / else` · `player.onChat` · 사칙연산 · 비교(`<` `>` `==`) · 전역 변수 · `변수 = 변수 + 1`

**안 쓴다**
화살표 함수 · 객체·배열 리터럴 · `while (true)` · `const` · 템플릿 리터럴 · `switch` · 사용자 정의 `function` · `!` 부정 연산자(대신 `else` 분기)

**채팅 매개변수는 `num1` `num2` `num3` 이름을 그대로 쓴다.**
MakeCode가 블록에서 생성하는 이름과 같아야 왕복 변환이 안전하다. 명령 안에서 곧바로 뜻이 담긴 지역 변수(`let steps = num1`)에 옮겨 담아 쓴다. 이 옮겨 담기가 방어 장치(`if (steps < 1)`)를 넣는 자리도 된다.

**방향 상수는 축약형으로 통일한다.**
`FORWARD` `BACK` `LEFT` `RIGHT` `UP` `DOWN` · `RIGHT_TURN` `LEFT_TURN` · `NORTH` `SOUTH` `EAST` `WEST`

**검증 필요** — `agent.teleport(world(x, y, z), WEST)` 와 `onChat` 매개변수는 1탄에 없던 API다. 첫 수업 전 반드시 MakeCode에서 실행해 확인한다. `checklist.md` 참고.

---

## 6. 운영 기준

- 대상: 초등 고학년 (5~6학년), **1탄 수료생**
- 12차시 × 90분
- 시간 배분: 도입 10분 / 전개 55분 / 정리 25분
- 아이 교재: 웹 교재 사이트 (GitHub Pages). 인쇄물 없음
- 담당: 박성주 (폭스러닝센터 AI 연구소)

## 7. 산출물 위치

| 대상 | 위치 |
|---|---|
| 아이 (웹 교재) | `docs/` → https://sungjjoo.github.io/fox-signal-12/ |
| 연구원 (강의지도안) | `lesson-plans/` |
| 연구원 (코드) | `code/` |
| 연구원 (월드) | `world/` |
| 연구원 (운영) | `operations/` |
| 기록 백업 (선택) | `sheet/` — 구글 시트 Apps Script |

### 아이 기록은 어떻게 저장되는가

`localStorage` 는 **기기 + 브라우저 프로필**에 묶인다. 아이 계정과는 무관하다.
그래서 이름으로 칸을 나눈다. 키는 `signal12:data:<이름>` 이다.

- 같은 노트북을 여러 아이가 써도 이름이 다르면 안 섞인다
- **이름을 넣기 전에는 입력칸이 잠긴다.** 익명 기록이 쌓여 섞이는 걸 막는다
- 노트북이 바뀌면 브라우저 저장만으로는 복구가 안 된다 → `sheet/` 의 구글 시트 백업이 그 대비다

시트를 연결하면 이름 입력 시 지난 기록을 내려받고(로컬에 없는 항목만 채운다), 입력할 때마다 1.5초 뒤 올린다.
연구원은 시트 메뉴 `AI 연구소 → 진도표 새로 만들기` 로 아이 × 층 진도표를 만든다.

**첫 차시에 이름 표기를 통일하는 것이 이 구조의 전제다.** 「박서준」과 「서준」은 다른 사람이 된다.

`docs/data/floors.js` 는 `docs/data/build_floors.py` 로 생성한다.
**코드 부분은 `code/floor-NN.js` 를 그대로 읽어온다.** 코드를 고쳤으면 스크립트를 다시 돌린다.
