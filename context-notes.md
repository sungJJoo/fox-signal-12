# 설계 맥락 노트 — 지하 12층, 신호를 따라서

이 문서는 프로그램 전체의 **단일 기준값**이다. 좌표·변수·명령어를 바꿀 때는 반드시 여기부터 고친다.

---

## 1. 왜 새로 만드는가

이전 프로그램 「에이전트와 함께해요」(16차시)의 한계를 세 가지로 정리했다.

| 한계 | 원인 | 이번 버전의 해결 |
|---|---|---|
| 확장성이 없다 | 문법 축이 `for`·`if` 둘뿐이라 아이가 할 수 있는 건 "숫자 바꾸기"뿐이었다 | **좌표**와 **변수**를 축으로 추가한다. 변수 하나를 바꾸면 결과 전체가 달라지는 경험을 매 차시에 넣는다 |
| 아이가 볼 교재가 없다 | 명령어 카드 PPT의 코드 스크린샷 자리가 비어 있었고, 기록 일지는 6칸이 매 차시 똑같았다 | **웹 교재 사이트**를 만든다. 층마다 신호·임무·코드·관찰 질문·확장 과제가 다르다 |
| 스토리가 지도안에만 있었다 | `오늘의 이야기` 한 줄이 코드·월드와 이어지지 않았다 | **층 = 차시 = Y좌표**를 1:1로 묶는다. 이야기가 진행되면 좌표가 내려가고, 좌표가 내려가면 코드가 어려워진다 |

---

## 2. 세계관

> 폭스 연구소 지하에는 30년 전 봉인된 시설이 있다. 기록은 남지 않았다.
> 3주 전부터 그 아래에서 신호가 올라온다. 신호는 언제나 숫자 세 개만 반복한다.
> 연구소는 그것이 좌표라는 것을 알아냈다. 하지만 사람은 내려갈 수 없다.
> 내려갈 수 있는 건 에이전트뿐이다.
> 너는 지상 관제실의 오퍼레이터다. 화면과 채팅창만으로 에이전트를 지하 12층까지 내려보내야 한다.

- 아이의 역할: **관제실 오퍼레이터**. 직접 내려가지 않는다. 이 제약이 "명령으로만 조종한다"를 자연스럽게 만든다.
- 12층에 있는 것: **30년 전 봉인된 최초의 에이전트**. 신호는 좌표를 보내며 "여기 있다"고 부른 것이었다.
- 이 결말은 이전 프로그램 「에이전트와 함께해요」와 이어진다. 수료생이 다시 들어올 이유가 된다.

---

## 3. 좌표 규칙 (전 차시 관통)

### 층 → Y 변환

```
Y = 62 - (층 × 4)
```

| 층 | Y | 층 | Y |
|---|---|---|---|
| 1 | 58 | 7 | 34 |
| 2 | 54 | 8 | 30 |
| 3 | 50 | 9 | 26 |
| 4 | 46 | 10 | 22 |
| 5 | 42 | 11 | 18 |
| 6 | 38 | 12 | 14 |

이 규칙 하나가 프로그램 전체를 관통한다.
5차시에서 아이가 이 식을 코드로 쓰는 순간이 **확장성을 체감하는 정점**이다. `floor` 변수만 바꾸면 어느 층이든 간다.

### 수직 갱도 (관제 통로)

- X = **-12**, Z = **8** 고정
- 내부 3×3 (X -13~-11, Z 7~9), Y 14 ~ 79

### 층별 신호 좌표

Y는 규칙으로 계산되지만 **X·Z는 층마다 다르다.** 아이가 매 차시 좌표를 새로 읽어야 하는 이유다.

| 층 | X | Y | Z | 층 상황 |
|---|---|---|---|---|
| 1 | -12 | 58 | 8 | 갱도 입구. 관제 개시 |
| 2 | -12 | 54 | 14 | 첫 하강 |
| 3 | -4 | 50 | 8 | 신호 지점이 갱도에서 떨어져 있다 |
| 4 | -20 | 46 | 8 | 반대쪽 |
| 5 | -12 | 42 | -2 | Z가 음수로 넘어간다 |
| 6 | 0 | 38 | 16 | 조명이 꺼져 있다 |
| 7 | -24 | 34 | 20 | 통로가 무너져 있다 |
| 8 | -6 | 30 | -10 | 물이 찼다 |
| 9 | -28 | 26 | 4 | 바닥이 없다 |
| 10 | 0 | 22 | 22 | 지나온 길을 표시해야 한다 |
| 11 | -18 | 18 | -12 | 신호가 강해진다 |
| 12 | -12 | 14 | 8 | 갱도 바로 아래. 계속 발밑에 있었다 |

### 층 홀 범위 (월드 빌드용)

- 내부 공간: X **-30 ~ 2**, Z **-14 ~ 24**, Y `층Y` ~ `층Y + 2`
- 바닥: `층Y - 1`
- 돌 외벽: X -31 ~ 3, Z -15 ~ 25, Y `층Y - 2` ~ `층Y + 3`

층 간격 4 = 바닥 1칸 + 공간 3칸. 딱 맞는다.

---

## 4. 코딩 범위

### 넣는 것

| 축 | 도입 차시 | 이유 |
|---|---|---|
| 좌표 읽기 (X·Y·Z) | 1 | 신호가 좌표다 |
| 상대 이동 (`move`) | 2 | 한 층 내려가기 |
| 절대 좌표 투입 (`teleport` + `world()`) | 3 | 관제실에서 지점 지정 |
| 변수 (숫자에 이름 붙이기) | 4 | 좌표를 매번 새로 쓰는 게 귀찮다 |
| 변수 계산 (`62 - floor * 4`) | 5 | 규칙을 코드로 만든다 |
| 반복 + 변수 조합 | 6, 8, 9 | 개수·길이·크기를 변수로 |
| 조건 (`if` / `detect`) | 7 | 무너진 통로 |
| 슬롯 전환 | 6, 10 | 재료 고르기 |
| 변수 누적 (`floor = floor + 1`) | 11 | 층을 순차 스캔 |
| 전체 통합 | 12 | 나만의 관제 루틴 |

### 빼는 것과 이유

- **함수** — 초등 고학년에게 "묶어서 이름 붙이기"는 변수와 개념이 겹쳐 혼동을 만든다. `onChat` 자체가 이미 함수 역할을 한다.
- **배열** — 인덱스 개념이 좌표(X·Y·Z)와 충돌한다. 좌표 학습을 흐린다.

### MakeCode 블록 변환 제약 (중요)

코드는 **MakeCode JS 탭에 붙여넣어 블록으로 변환**해서 아이에게 보여준다.
따라서 static TypeScript 서브셋만 쓴다.

**쓴다**
`let` · `for (let i = 0; i < n; i++)` · `if / else` · `player.onChat` · 사칙연산 · 전역 변수 선언

**안 쓴다** (블록 변환이 깨지거나 회색 JS 블록이 된다)
화살표 함수 · 객체 리터럴 · 배열 리터럴 · `while (true)` · `const` · 템플릿 리터럴 (`` `${}` ``) · `switch` · 사용자 정의 `function`

**방향 상수는 축약형을 쓴다** — MakeCode가 블록에서 생성하는 형태와 같아 왕복 변환이 안전하다.
`FORWARD` `BACK` `LEFT` `RIGHT` `UP` `DOWN` · `RIGHT_TURN` `LEFT_TURN` · `NORTH` `SOUTH` `EAST` `WEST`
(`SixDirection.Forward` 형태도 동작하지만 한 저장소 안에서 섞지 않는다.)

**검증 필요** — `agent.teleport(world(x, y, z), WEST)` 는 이번에 새로 도입하는 API다. 첫 수업 전 반드시 MakeCode에서 한 번 실행해 확인한다. `world()` 는 절대 좌표, `pos()` 는 플레이어 기준 상대 좌표다. 헷갈리면 아이가 엉뚱한 곳으로 보낸다.

---

## 5. 운영 기준

- 대상: 초등 고학년 (5~6학년)
- 12차시 × 90분
- 시간 배분: 도입 10분 / 전개 55분 / 정리 25분
- 아이 교재: 웹 교재 사이트 (GitHub Pages). 인쇄물 없음
- 담당: 박성주 (폭스러닝센터 AI 연구소)

## 6. 산출물 위치

| 대상 | 위치 |
|---|---|
| 아이 (웹 교재) | `docs/` → https://sungjjoo.github.io/fox-signal-12/ |
| 연구원 (강의지도안) | `lesson-plans/` |
| 연구원 (코드) | `code/` |
| 연구원 (월드) | `world/` |
| 연구원 (운영) | `operations/` |
