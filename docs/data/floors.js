// 아이용 웹 교재의 12개 층 콘텐츠 원본
// build_floors.py 로 생성된다. 코드 부분은 code/floor-NN.js 를 그대로 가져온다.

window.FLOORS = [
  {"n": 1, "y": 58, "x": -22, "z": 18, "title": "다시 만난 에이전트", "subtitle": "절대 좌표", "learn": "world(x, y, z) 와 agent.teleport", "cmds": ["back", "go", "near", "sig", "look"], "story": "1탄에서 너는 에이전트에게 명령하는 법을 배웠어. 기지를 짓고 광물을 캤지.\n그때 에이전트는 언제나 네 옆에 있었어.\n\n그런데 폭스 연구소 지하에서 신호가 올라오기 시작했어.\n30년 전 봉인된 시설. 기록은 남지 않았어.\n\n사람은 내려갈 수 없어. 에이전트도 네 옆에 있으면 못 내려가.\n옆이 아니라 좌표로 보내야 해.", "mission": ["1탄에서 만든 코드를 그대로 한 번 실행해본다", "back(1탄 방식)과 go(절대 좌표)를 나란히 만들어 번갈아 실행한다", "near 를 만든다. 숫자는 같고 world 만 pos 로 바꾼 것", "내 자리를 세 군데로 옮겨가며 go 와 near 를 각각 실행하고 좌표를 적는다", "sig 의 좌표는 혼자 채운다", "look 을 만든다. 한 덩어리를 완성하고 복사해서 방향만 바꾼다", "갱도 안과 홀 한가운데에서 look 을 각각 실행해 비교한다"], "observe": ["go 를 세 군데에서 실행했을 때 도착 좌표는? (세 개 다 적어봐)", "near 를 세 군데에서 실행했을 때 도착 좌표는?", "world 와 pos 의 차이를 한 문장으로", "갱도 안에서 look 결과는? 열린 방향이 몇 개야?", "홀 한가운데서는?"], "stretch": ["WEST 를 EAST 로 바꾸면 도착 위치가 달라질까, 바라보는 방향만 달라질까", "없는 좌표(Y를 300)로 보내면 에이전트는 어떻게 될까. back 으로 돌아올까", "look 이 말 대신 열린 방향 개수를 세게 하려면 뭐가 더 필요할까"], "tip": "에이전트가 화면에서 사라져도 당황하지 마. 지하에 있는 거야. back 을 치면 돌아와.", "code": "player.onChat(\"back\", function () {\n    agent.teleportToPlayer()\n    loops.pause(400)\n    player.say(\"에이전트 소환\")\n})\n\n// [go] 갱도 입구로 절대 좌표 투입\nplayer.onChat(\"go\", function () {\n    agent.teleport(world(-12, 58, 8), WEST)\n    loops.pause(400)\n    player.say(\"갱도 입구 도착\")\n})\n\n// [near] 같은 숫자를 상대 좌표로 준다. go 와 무엇이 다른지 비교한다\nplayer.onChat(\"near\", function () {\n    agent.teleport(pos(-12, 58, 8), WEST)\n    loops.pause(400)\n    player.say(\"상대 좌표 이동\")\n})\n\n// [sig] 1층 신호 지점으로 투입\nplayer.onChat(\"sig\", function () {\n    agent.teleport(world(-22, 58, 18), WEST)\n    loops.pause(400)\n    player.say(\"1층 신호 지점 도착\")\n})\n\n// [look] 여섯 방향을 모두 감지해서 보고한다\n// 1탄의 see 는 네 방향이었고 막혔을 때만 말했다. 이제 열렸을 때도 말한다\nplayer.onChat(\"look\", function () {\n    if (agent.detect(AgentDetection.Block, FORWARD)) {\n        player.say(\"앞 막힘\")\n    } else {\n        player.say(\"앞 열림\")\n    }\n    loops.pause(200)\n    if (agent.detect(AgentDetection.Block, BACK)) {\n        player.say(\"뒤 막힘\")\n    } else {\n        player.say(\"뒤 열림\")\n    }\n    loops.pause(200)\n    if (agent.detect(AgentDetection.Block, LEFT)) {\n        player.say(\"왼쪽 막힘\")\n    } else {\n        player.say(\"왼쪽 열림\")\n    }\n    loops.pause(200)\n    if (agent.detect(AgentDetection.Block, RIGHT)) {\n        player.say(\"오른쪽 막힘\")\n    } else {\n        player.say(\"오른쪽 열림\")\n    }\n    loops.pause(200)\n    if (agent.detect(AgentDetection.Block, UP)) {\n        player.say(\"위 막힘\")\n    } else {\n        player.say(\"위 열림\")\n    }\n    loops.pause(200)\n    if (agent.detect(AgentDetection.Block, DOWN)) {\n        player.say(\"아래 막힘\")\n    } else {\n        player.say(\"아래 열림\")\n    }\n    player.say(\"탐색 완료\")\n})"},
  {"n": 2, "y": 54, "x": -4, "z": -6, "title": "숫자를 실어 보낸다", "subtitle": "채팅 명령 매개변수", "learn": "onChat 의 num1", "cmds": ["dive", "rise", "fwd", "dig"], "story": "2층 신호가 잡혔어. 그런데 층마다 내려갈 깊이가 달라.\n\n3층으로 보낼 때, 7층으로 보낼 때, 매번 코드를 열어 숫자를 고칠 거야?\n1탄에서 그렇게 했잖아. 열고, 찾고, 고치고, 저장하고, 돌아가고. 다섯 단계.\n\n관제실 무전은 그렇게 하지 않아. 명령에 숫자를 실어 보내.", "mission": ["on chat command 블록에 마우스를 올려 num1 을 찾는다", "player.say(num1) 만 넣고 dive 7 을 쳐본다. 숫자가 들어오는 걸 먼저 확인", "숫자 없이 dive 만 쳐본다. 0이 나온다", "방어 장치를 넣는다. 1보다 작으면 1로", "dive 를 완성한다. 한 층은 4칸이니까 num1 x 4", "rise fwd dig 를 같은 패턴으로 만든다. 방어 장치도 같이"], "observe": ["dive 1 → Y가 얼마나 줄었어?", "dive 3 → 예상 / 실제", "dive 5 → 예상 / 실제", "숫자 없이 dive 만 쳤을 때 무슨 일이 일어났어?", "1탄에서 숫자 바꾸던 방법과 오늘 방법을 비교해서 한 줄로"], "stretch": ["dive 100 을 치면? 어디서 멈추고 왜 그럴까", "dive -3 처럼 음수를 넣으면? 방어 장치가 막아줄까", "dig 에서 collectAll 을 빼면 뭐가 달라질까"], "tip": "명령어와 숫자 사이에 띄어쓰기 한 칸. dive3 은 안 되고 dive 3 이라야 해.", "code": "player.onChat(\"dive\", function (num1) {\n    let downFloors = num1\n    if (downFloors < 1) {\n        downFloors = 1\n    }\n    for (let i = 0; i < downFloors * 4; i++) {\n        agent.move(DOWN, 1)\n        loops.pause(250)\n    }\n    player.say(downFloors)\n    player.say(\"층 하강 완료\")\n})\n\n// [rise] num1 층만큼 올라온다.  예)  rise 2\nplayer.onChat(\"rise\", function (num1) {\n    let upFloors = num1\n    if (upFloors < 1) {\n        upFloors = 1\n    }\n    for (let i = 0; i < upFloors * 4; i++) {\n        agent.move(UP, 1)\n        loops.pause(250)\n    }\n    player.say(upFloors)\n    player.say(\"층 상승 완료\")\n})\n\n// [fwd] 앞으로 num1 칸 간다.  예)  fwd 10\nplayer.onChat(\"fwd\", function (num1) {\n    let steps = num1\n    if (steps < 1) {\n        steps = 1\n    }\n    for (let i = 0; i < steps; i++) {\n        agent.move(FORWARD, 1)\n        loops.pause(250)\n    }\n    player.say(steps)\n})\n\n// [dig] 앞으로 num1 칸 파며 전진한다.  예)  dig 8\nplayer.onChat(\"dig\", function (num1) {\n    let steps = num1\n    if (steps < 1) {\n        steps = 1\n    }\n    for (let i = 0; i < steps; i++) {\n        if (agent.detect(AgentDetection.Block, FORWARD)) {\n            agent.destroy(FORWARD)\n            loops.pause(250)\n            agent.collectAll()\n            loops.pause(250)\n        }\n        agent.move(FORWARD, 1)\n        loops.pause(250)\n    }\n    player.say(\"굴착 완료\")\n})\n\n// [sig2] 2층 신호 지점으로 투입\nplayer.onChat(\"sig2\", function () {\n    agent.teleport(world(-4, 54, -6), WEST)\n    loops.pause(400)\n    player.say(\"2층 신호 지점 도착\")\n})"},
  {"n": 3, "y": 50, "x": -26, "z": 2, "title": "좌표를 채팅으로", "subtitle": "매개변수 두세 개", "learn": "num1 · num2 · num3", "cmds": ["jump", "warp", "deck", "tower"], "slot": "슬롯 1 → 석재 64개", "story": "3층은 갱도에서 멀리 떨어진 곳에서 신호가 와.\n어제 만든 dive 로는 아래로만 갈 수 있어.\n\n좌표는 숫자 세 개야.\n세 개를 다 실어 보낼 수 있다면 명령 하나로 이 지하 시설 어디든 갈 수 있어.", "mission": ["jump 를 만든다. X와 Z만 받고 Y는 코드에 고정", "연구원이 부르는 좌표를 넣고, 실행 전에 화면 어디쯤 나올지 손으로 가리킨다", "warp 를 만든다. 좌표 세 개를 전부 받는다", "warp -12 14 8 을 쳐본다. 아직 아무도 안 가본 12층이다", "deck 을 만든다. 1탄 box 와 구조는 같고 크기만 채팅으로 받는다", "deck 4 / deck 6 / deck 9 를 연달아 친다. 실행 전에 필요 블록 수를 계산한다", "tower 를 만든다. 높이를 채팅으로 받는다"], "observe": ["warp 로 가본 층 세 곳의 좌표를 적어봐", "deck 4 → 예상 블록 수 / 실제", "deck 6 → 예상 / 실제", "deck 9 → 예상 / 실제", "jump 와 warp 중 어느 쪽이 편했어? 언제 그래?"], "stretch": ["warp 에서 num1 자리에 Y값을, num2 자리에 X값을 넣으면 어디로 갈까. 왜?", "deck 20 을 치면? 블록은 몇 개 필요하고 홀 밖으로 나갈까", "tower 에서 setSlot 을 빼면 무슨 일이 일어날까"], "tip": "숫자 사이에도 띄어쓰기. jump -26 2 처럼 마이너스 부호도 붙여서 한 덩어리로 쳐.", "code": "player.onChat(\"jump\", function (num1, num2) {\n    agent.teleport(world(num1, 50, num2), WEST)\n    loops.pause(400)\n    player.say(\"3층 이동 완료\")\n})\n\n// [warp] 좌표 세 개를 전부 채팅으로.  예)  warp -12 42 8\nplayer.onChat(\"warp\", function (num1, num2, num3) {\n    agent.teleport(world(num1, num2, num3), WEST)\n    loops.pause(400)\n    player.say(\"좌표 투입 완료\")\n})\n\n// [deck] 한 변이 num1 칸인 네모 발판을 만든다.  예)  deck 6\n// 1탄에서 만들던 box 와 구조는 같다. 크기를 채팅으로 준다는 것만 다르다\nplayer.onChat(\"deck\", function (num1) {\n    let side = num1\n    if (side < 2) {\n        side = 2\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let turn = 0; turn < 4; turn++) {\n        for (let i = 0; i < side; i++) {\n            if (agent.detect(AgentDetection.Block, DOWN)) {\n                agent.destroy(DOWN)\n                loops.pause(250)\n            }\n            agent.place(DOWN)\n            loops.pause(250)\n            agent.move(FORWARD, 1)\n            loops.pause(250)\n        }\n        agent.turn(RIGHT_TURN)\n        loops.pause(250)\n    }\n    player.say(side)\n    player.say(\"발판 완성\")\n})\n\n// [tower] 높이 num1 짜리 사각 기둥을 세운다.  예)  tower 8\nplayer.onChat(\"tower\", function (num1) {\n    let height = num1\n    if (height < 1) {\n        height = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let f = 0; f < height; f++) {\n        for (let w = 0; w < 4; w++) {\n            agent.place(FORWARD)\n            loops.pause(250)\n            agent.turn(RIGHT_TURN)\n            loops.pause(250)\n        }\n        agent.move(UP, 1)\n        loops.pause(250)\n    }\n    player.say(height)\n    player.say(\"층 기둥 완성\")\n})\n\n// [sig3] 3층 신호 지점으로 투입\nplayer.onChat(\"sig3\", function () {\n    agent.teleport(world(-26, 50, 2), EAST)\n    loops.pause(400)\n    player.say(\"3층 신호 지점 도착\")\n})"},
  {"n": 4, "y": 46, "x": 0, "z": 20, "title": "값을 기억시킨다", "subtitle": "변수", "learn": "let 변수", "cmds": ["set", "show", "move", "mark", "home"], "slot": "슬롯 2 → 눈에 띄는 색 블록", "story": "관제 기록관이 보고서를 반려했어.\n\n\"warp 로 갔다며. 그런데 deck 은 그게 어딘지 몰라. mark 도 몰라.\n명령마다 좌표를 따로 치고 있잖아. 하나라도 틀리면 딴 데다 지어.\"\n\n명령이 끝나면 num1 은 사라져. 남는 값이 필요해.", "mission": ["warp → deck → 표식 순서로 미션을 해본다. 좌표를 세 번 친다", "다른 층으로 옮겨서 또 해본다. 또 세 번 친다. 누군가는 틀린다", "변수 세 개를 onChat 바깥, 맨 위에 만든다", "일부러 onChat 안에 만들어보고 다른 명령에서 안 보이는 걸 확인한다", "set 과 show 를 만든다. set 한 뒤 show 를 치면 숫자 없이도 대답한다", "move mark home 을 만든다. 셋 다 좌표를 안 받는다", "set 한 줄만 바꿔서 세 명령을 그대로 다시 실행한다"], "observe": ["내가 지은 변수 이름 세 개는?", "왜 그렇게 이름 지었어?", "set 한 번으로 몇 개의 명령이 움직였어?", "num1 과 변수의 차이를 한 문장으로", "set 을 안 치고 show 를 치면 뭐가 나와? 그 숫자는 어디서 왔을까?"], "stretch": ["변수를 onChat 안으로 옮기면 어떤 오류가 날까. 왜?", "home 은 targetY 만 써. X와 Z는 왜 고정일까", "ring 을 만들어봐. 기억한 좌표 사방에 표식 네 개. targetX + gap 처럼 계산을 붙인다"], "tip": "변수는 맨 위 바깥에 만들어. onChat 안에 만들면 명령이 끝날 때 같이 사라져.", "code": "let targetX = 0\nlet targetY = 46\nlet targetZ = 20\n\n// [set] 목표 좌표를 기억시킨다.  예)  set 0 46 20\nplayer.onChat(\"set\", function (num1, num2, num3) {\n    targetX = num1\n    targetY = num2\n    targetZ = num3\n    player.say(\"좌표 기억 완료\")\n})\n\n// [show] 기억한 좌표를 보고한다. 숫자를 안 줘도 대답한다\nplayer.onChat(\"show\", function () {\n    player.say(targetX)\n    loops.pause(300)\n    player.say(targetY)\n    loops.pause(300)\n    player.say(targetZ)\n})\n\n// [move] 기억한 좌표로 이동한다. 좌표를 다시 칠 필요가 없다\nplayer.onChat(\"move\", function () {\n    agent.teleport(world(targetX, targetY, targetZ), WEST)\n    loops.pause(400)\n    player.say(\"기억한 좌표로 이동\")\n})\n\n// [mark] 기억한 좌표에 표식을 설치한다. move 와 같은 값을 쓴다\nplayer.onChat(\"mark\", function () {\n    agent.teleport(world(targetX, targetY, targetZ), WEST)\n    loops.pause(400)\n    agent.setSlot(2)\n    loops.pause(200)\n    agent.destroy(DOWN)\n    loops.pause(250)\n    agent.place(DOWN)\n    loops.pause(250)\n    player.say(\"표식 설치 완료\")\n})\n\n// [home] 기억한 층의 갱도 입구로 복귀한다. Y만 기억한 값을 쓴다\nplayer.onChat(\"home\", function () {\n    agent.teleport(world(-12, targetY, 8), WEST)\n    loops.pause(400)\n    player.say(\"갱도 복귀\")\n})\n\n// [ring] 기억한 좌표를 중심으로 표식 네 개를 num1 칸 떨어뜨려 놓는다.  예)  ring 5\nplayer.onChat(\"ring\", function (num1) {\n    let gap = num1\n    if (gap < 1) {\n        gap = 1\n    }\n    agent.setSlot(2)\n    loops.pause(200)\n    agent.teleport(world(targetX + gap, targetY, targetZ), WEST)\n    loops.pause(350)\n    agent.destroy(DOWN)\n    loops.pause(250)\n    agent.place(DOWN)\n    loops.pause(250)\n    agent.teleport(world(targetX - gap, targetY, targetZ), WEST)\n    loops.pause(350)\n    agent.destroy(DOWN)\n    loops.pause(250)\n    agent.place(DOWN)\n    loops.pause(250)\n    agent.teleport(world(targetX, targetY, targetZ + gap), WEST)\n    loops.pause(350)\n    agent.destroy(DOWN)\n    loops.pause(250)\n    agent.place(DOWN)\n    loops.pause(250)\n    agent.teleport(world(targetX, targetY, targetZ - gap), WEST)\n    loops.pause(350)\n    agent.destroy(DOWN)\n    loops.pause(250)\n    agent.place(DOWN)\n    loops.pause(250)\n    player.say(\"사방 표식 완료\")\n})"},
  {"n": 5, "y": 42, "x": -12, "z": -10, "key": true, "title": "층 계산기", "subtitle": "변수끼리 계산한다", "learn": "규칙을 식으로 만들기", "cmds": ["depth", "floor", "at", "build"], "slot": "슬롯 1 → 석재 64개", "story": "관제실 기록관이 지금까지의 층별 Y를 표로 붙여놨어.\n\n58 · 54 · 50 · 46 · 42 ...\n\n\"매번 표에서 찾아 칠 거야? 12층 Y가 뭔지는 알아?\"", "mission": ["컴퓨터를 덮는다. 종이에 1~4층 Y를 적는다", "층이 1 늘 때마다 Y가 몇씩 변하는지 찾는다", "0층이 있다면 Y가 몇일지 거꾸로 계산한다", "식을 만든다. Y = 62 - 층 x 4", "종이로 검산한다. 3층은 50? 12층은?", "depth 를 만든다. 계산만 하고 이동은 안 한다", "일부러 62 - num1 을 먼저 하고 x 4 를 해본다. 종이 값과 비교", "floor 를 만든다. floor 1 부터 floor 12 까지 전부 쳐본다", "at 을 만든다. 층과 X, Z를 함께 받는다"], "observe": ["층이 1 늘 때마다 Y는 몇씩 변해?", "식을 그대로 적어봐", "1층부터 12층까지 Y를 계산해서 적어봐 (게임으로 확인하기 전에!)", "연산 순서를 틀렸을 때 나온 값 / 맞는 값", "3차시 warp 와 오늘 floor 의 차이를 한 문장으로"], "stretch": ["지하 20층이 있다면 Y는? 계산하면 -18이야. 넣으면 어떻게 될까. 왜?", "층 간격이 4가 아니라 6이라면 식이 어떻게 바뀔까. 코드에서 4를 6으로 바꿔봐", "62 를 다른 숫자로 바꾸면 뭐가 달라져? 62는 무슨 뜻이야?"], "tip": "오늘이 제일 중요한 날이야. 아직 아무도 안 가본 12층 좌표를 계산으로 먼저 알아냈어.", "code": "let floorNow = 5\n\n// [depth] 층 번호를 주면 Y만 계산해서 알려준다. 이동은 안 한다.  예)  depth 9\nplayer.onChat(\"depth\", function (num1) {\n    let y = 62 - num1 * 4\n    player.say(num1)\n    loops.pause(300)\n    player.say(y)\n})\n\n// [floor] 층 번호만 주면 계산해서 그 층 갱도로 투입한다.  예)  floor 9\nplayer.onChat(\"floor\", function (num1) {\n    floorNow = num1\n    let y = 62 - floorNow * 4\n    agent.teleport(world(-12, y, 8), WEST)\n    loops.pause(500)\n    player.say(floorNow)\n    loops.pause(300)\n    player.say(y)\n})\n\n// [at] 층과 X, Z 를 함께 준다. Y는 코드가 계산한다.  예)  at 7 -24 20\nplayer.onChat(\"at\", function (num1, num2, num3) {\n    let y = 62 - num1 * 4\n    agent.teleport(world(num2, y, num3), WEST)\n    loops.pause(500)\n    player.say(\"투입 완료\")\n    loops.pause(300)\n    player.say(y)\n})\n\n// [build] num1 층 갱도 옆에 한 변 num2 짜리 발판을 만든다.  예)  build 9 6\n// 층 계산과 1탄에서 하던 발판 만들기를 하나로 잇는다\nplayer.onChat(\"build\", function (num1, num2) {\n    let y = 62 - num1 * 4\n    let side = num2\n    if (side < 2) {\n        side = 2\n    }\n    agent.teleport(world(-12, y, 8), WEST)\n    loops.pause(500)\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let turn = 0; turn < 4; turn++) {\n        for (let i = 0; i < side; i++) {\n            if (agent.detect(AgentDetection.Block, DOWN)) {\n                agent.destroy(DOWN)\n                loops.pause(250)\n            }\n            agent.place(DOWN)\n            loops.pause(250)\n            agent.move(FORWARD, 1)\n            loops.pause(250)\n        }\n        agent.turn(RIGHT_TURN)\n        loops.pause(250)\n    }\n    player.say(y)\n    player.say(\"발판 완성\")\n})\n\n// [sig5] 5층 신호 지점으로 투입\nplayer.onChat(\"sig5\", function () {\n    agent.teleport(world(-12, 42, -10), SOUTH)\n    loops.pause(400)\n    player.say(\"5층 신호 지점 도착\")\n})"},
  {"n": 6, "y": 38, "x": 0, "z": 16, "key": true, "shot": true, "title": "반복 변수를 좌표에 쓴다", "subtitle": "i 를 계산의 재료로", "learn": "for 의 i 를 좌표에 넣기", "cmds": ["row", "col", "diag", "drill"], "slot": "슬롯 1 → 글로우스톤 64개", "story": "6층은 조명이 전부 죽어 있어. 화면이 새까매.\n\n유도등을 심어야 하는데 아무렇게나 놓으면 안 돼.\n정확한 간격으로 놓아야 관제실이 위치를 읽을 수 있어.", "mission": ["1탄 방식(이동하며 놓기)을 먼저 실행한다. 여기서 i 는 아무 일도 안 한다", "종이에 계산해본다. i=0 → -28+0x3, i=1 → -28+1x3, i=2 → ...", "row 를 만든다. 실행 전에 여섯 개 X좌표를 전부 종이에 적는다", "실행하고 화면 좌표와 대조한다", "두 방식을 같은 층에서 실행하고 비교표를 채운다", "col 을 만든다. row 를 복사해 i x gap 을 Z 자리로", "diag 를 만든다. i 를 X와 Z에 동시에. 관제 홀을 피해 남서쪽에서 시작한다", "Y에도 i 를 넣어본다. 왜 안 되는지 찾는다", "drill 을 만든다. i 로 층 번호를 만들고 그 층 번호로 Y를 계산한다"], "observe": ["row 6 3 의 여섯 X좌표 — 계산값 / 실제값", "이동해서 놓기 vs 좌표 찍어서 놓기 — 중간에 벽이 있으면?", "정확한 위치를 아는 건 어느 쪽?", "Y에 i 를 넣었더니 왜 안 됐어?", "drill 에서 계산이 몇 번 일어나?"], "stretch": ["row 의 시작 좌표 -28 을 변수로 빼면 뭐가 좋아질까", "i * gap 을 gap * i 로 바꾸면 결과가 달라질까. 왜?", "row 와 col 을 연달아 실행해 십자를 만들려면 시작 좌표를 어떻게 맞춰야 할까"], "tip": "1탄에서 i 는 몇 번 했는지 세기만 했어. 오늘부터 i 는 자리를 만드는 재료야.", "code": "player.onChat(\"row\", function (num1, num2) {\n    let count = num1\n    let gap = num2\n    if (count < 1) {\n        count = 1\n    }\n    if (gap < 1) {\n        gap = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < count; i++) {\n        agent.teleport(world(-28 + i * gap, 38, 16), WEST)\n        loops.pause(350)\n        agent.destroy(DOWN)\n        loops.pause(250)\n        agent.place(DOWN)\n        loops.pause(250)\n    }\n    player.say(\"가로 설치 완료\")\n})\n\n// [col] Z 방향으로 num1 개를 num2 칸 간격으로 심는다.  예)  col 5 4\nplayer.onChat(\"col\", function (num1, num2) {\n    let count = num1\n    let gap = num2\n    if (count < 1) {\n        count = 1\n    }\n    if (gap < 1) {\n        gap = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < count; i++) {\n        agent.teleport(world(-12, 38, -12 + i * gap), WEST)\n        loops.pause(350)\n        agent.destroy(DOWN)\n        loops.pause(250)\n        agent.place(DOWN)\n        loops.pause(250)\n    }\n    player.say(\"세로 설치 완료\")\n})\n\n// [diag] i 를 X와 Z 두 곳에 함께 쓴다. 대각선으로 놓인다.  예)  diag 10\n// 층 높이가 3칸뿐이라 Y에는 i 를 쓸 수 없다. 왜 그런지 생각해보자\n// 관제 홀을 통과하지 않도록 남서쪽 빈 구역에서 시작한다\nplayer.onChat(\"diag\", function (num1) {\n    let steps = num1\n    if (steps < 1) {\n        steps = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < steps; i++) {\n        agent.teleport(world(-28 + i, 38, -12 + i), WEST)\n        loops.pause(350)\n        agent.destroy(DOWN)\n        loops.pause(250)\n        agent.place(DOWN)\n        loops.pause(250)\n    }\n    player.say(\"대각선 설치 완료\")\n})\n\n// [drill] 1층부터 num1 층까지 한 층씩 내려가며 좌표를 보고한다.  예)  drill 6\n// i 로 층 번호를 만들고, 그 층 번호로 다시 Y를 계산한다\nplayer.onChat(\"drill\", function (num1) {\n    let last = num1\n    if (last < 1) {\n        last = 1\n    }\n    for (let i = 0; i < last; i++) {\n        let f = i + 1\n        let y = 62 - f * 4\n        agent.teleport(world(-12, y, 8), WEST)\n        loops.pause(600)\n        player.say(y)\n    }\n    player.say(\"하강 점검 완료\")\n})\n\n// [sig6] 6층 신호 지점으로 투입\nplayer.onChat(\"sig6\", function () {\n    agent.teleport(world(0, 38, 16), WEST)\n    loops.pause(400)\n    player.say(\"6층 진입. 조명 없음\")\n})"},
  {"n": 7, "y": 34, "x": -24, "z": 20, "title": "막힌 곳을 센다", "subtitle": "조건 안에서 변수 키우기", "learn": "if 안에서 변수를 1씩 키운다", "cmds": ["count", "clear", "free", "away"], "slot": "슬롯 1 → 글로우스톤", "story": "7층 통로는 군데군데 무너져 있어.\n뚫고 지나가는 건 1탄에서 해봤지. 문제는 그 다음이야.\n\n관제실은 보고를 받아야 해. 몇 군데가 막혀 있었는가.\n에이전트는 지나갔지만 아무것도 세지 않았어.", "mission": ["1탄 smart 를 그대로 실행한다. 잘 된다. 그런데 몇 군데 막혔는지는 아무도 모른다", "count 를 만든다. 조건이 참일 때마다 blocked 를 1씩 키운다", "sig7 로 간 뒤 실행 전에 막힌 곳 개수를 눈으로 예상해 적는다", "count 12 를 실행하고 맞춰본다", "맨 앞 초기화를 지우고 두 번 연속 실행해본다", "왜 두 번째는 값이 다른지 생각한다 (이미 다 부쉈다)", "free 를 만든다. 네 방향 중 몇 개가 열렸는지 숫자 하나로 답한다", "갱도 안과 홀 한가운데서 free 를 각각 실행한다"], "observe": ["count 12 — 내 예상 / 실제", "초기화를 지우고 두 번 실행했을 때 나온 숫자", "같은 구간을 두 번 재면 왜 값이 달라져?", "갱도에서 free 결과 / 홀에서 free 결과", "blocked 와 opened 를 더하면 항상 steps 와 같을까?"], "stretch": ["막힌 곳이 세 군데 이상이면 '위험'이라고 말하게 하려면 뭐가 필요할까", "count 를 아래 방향(DOWN)으로 바꾸면 뭘 세게 될까", "away 를 만들어봐. 막히면 부수지 않고 오른쪽으로 피해 간다"], "tip": "조건이 참일 때마다 변수를 1씩 키우면 그게 곧 개수가 돼.", "code": "let blocked = 0\nlet opened = 0\n\n// [count] num1 칸 가면서 막힌 곳이 몇 군데인지 세서 보고한다.  예)  count 12\nplayer.onChat(\"count\", function (num1) {\n    let steps = num1\n    if (steps < 1) {\n        steps = 1\n    }\n    blocked = 0\n    opened = 0\n    for (let i = 0; i < steps; i++) {\n        if (agent.detect(AgentDetection.Block, FORWARD)) {\n            blocked = blocked + 1\n            agent.destroy(FORWARD)\n            loops.pause(250)\n            agent.collectAll()\n            loops.pause(250)\n        } else {\n            opened = opened + 1\n        }\n        agent.move(FORWARD, 1)\n        loops.pause(250)\n    }\n    player.say(\"막힌 곳\")\n    loops.pause(300)\n    player.say(blocked)\n    loops.pause(300)\n    player.say(\"뚫린 곳\")\n    loops.pause(300)\n    player.say(opened)\n})\n\n// [clear] num1 칸 뚫으며 전진한다. 세지는 않는다.  예)  clear 15\nplayer.onChat(\"clear\", function (num1) {\n    let steps = num1\n    if (steps < 1) {\n        steps = 1\n    }\n    for (let i = 0; i < steps; i++) {\n        if (agent.detect(AgentDetection.Block, FORWARD)) {\n            agent.destroy(FORWARD)\n            loops.pause(250)\n            agent.collectAll()\n            loops.pause(250)\n        } else {\n            agent.move(FORWARD, 1)\n            loops.pause(250)\n        }\n    }\n    player.say(\"통로 확보\")\n})\n\n// [free] 지금 자리에서 열린 방향이 몇 개인지 센다\nplayer.onChat(\"free\", function () {\n    opened = 0\n    if (agent.detect(AgentDetection.Block, FORWARD)) {\n        player.say(\"앞 막힘\")\n    } else {\n        opened = opened + 1\n    }\n    if (agent.detect(AgentDetection.Block, BACK)) {\n        player.say(\"뒤 막힘\")\n    } else {\n        opened = opened + 1\n    }\n    if (agent.detect(AgentDetection.Block, LEFT)) {\n        player.say(\"왼쪽 막힘\")\n    } else {\n        opened = opened + 1\n    }\n    if (agent.detect(AgentDetection.Block, RIGHT)) {\n        player.say(\"오른쪽 막힘\")\n    } else {\n        opened = opened + 1\n    }\n    player.say(\"열린 방향\")\n    loops.pause(300)\n    player.say(opened)\n})\n\n// [away] 막히면 부수지 않고 오른쪽으로 돌아서 피해 간다.  예)  away 10\nplayer.onChat(\"away\", function (num1) {\n    let steps = num1\n    if (steps < 1) {\n        steps = 1\n    }\n    blocked = 0\n    for (let i = 0; i < steps; i++) {\n        if (agent.detect(AgentDetection.Block, FORWARD)) {\n            blocked = blocked + 1\n            agent.turn(RIGHT_TURN)\n            loops.pause(250)\n        } else {\n            agent.move(FORWARD, 1)\n            loops.pause(250)\n        }\n    }\n    player.say(\"우회 횟수\")\n    loops.pause(300)\n    player.say(blocked)\n})\n\n// [sig7] 7층 신호 지점으로 투입\nplayer.onChat(\"sig7\", function () {\n    agent.teleport(world(-24, 34, 20), EAST)\n    loops.pause(400)\n    player.say(\"7층 진입. 통로 붕괴\")\n})"},
  {"n": 8, "y": 30, "x": -6, "z": -10, "shot": true, "title": "면을 채운다", "subtitle": "반복 변수 두 개", "learn": "i 와 j 를 각각 다른 축에", "cmds": ["line", "raft", "need", "frame"], "slot": "슬롯 1 → 석재 64개", "story": "8층은 물에 잠겼어. 한 줄 다리로는 못 건너.\n관제실은 넓은 부교를 요구해.\n\n1탄에서 사각형은 만들어봤지. 하지만 그건 테두리였어.\n오늘은 안까지 꽉 찬 면을 만든다.", "mission": ["종이에 6x4 격자를 그리고 몇 칸인지 센다", "line 을 만든다. 6차시 방식이지만 시작 좌표를 변수로 뺀다", "raft 를 만든다. 반복 안에 반복을 넣고 i 와 j 를 X와 Z에 나눠 준다", "실행 전에 i, j 조합별 좌표를 표로 계산한다", "raft 6 4 를 실행하고 에이전트가 도는 순서를 눈으로 따라간다", "i 와 j 를 서로 바꿔 넣고 다시 실행한다", "need 를 만든다. 만들기 전에 재료가 되는지 확인하는 도구", "need 10 10 을 쳐본 뒤 raft 10 10 을 실행해본다"], "observe": ["i=0 j=0 일 때 X / Z 는?", "i=1 j=0 일 때는?", "i=2 j=3 일 때는?", "raft 6 4 와 raft 4 6 은 뭐가 달라?", "need 로 계산한 값 / 실제 사용한 블록 수", "안쪽 반복과 바깥 반복 중 뭐가 먼저 다 도는 거야?"], "stretch": ["raft 1 10 은 무엇과 같을까", "부교를 2층으로 쌓으려면 반복을 하나 더 겹쳐야 할까, 다른 방법이 있을까", "raft 에 destroy(DOWN) 이 필요한 곳과 필요 없는 곳은? (6층과 8층을 비교해봐)"], "tip": "안쪽 반복이 다 돌아야 바깥 반복이 한 칸 가. 에이전트가 도는 순서를 눈으로 따라가봐.", "code": "let baseX = -10\nlet baseZ = -8\n\n// [line] 한 줄만 깐다. 6층 방식 복습.  예)  line 10\nplayer.onChat(\"line\", function (num1) {\n    let len = num1\n    if (len < 1) {\n        len = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < len; i++) {\n        agent.teleport(world(baseX + i, 30, baseZ), WEST)\n        loops.pause(300)\n        agent.place(DOWN)\n        loops.pause(250)\n    }\n    player.say(\"한 줄 완성\")\n})\n\n// [raft] 가로 num1 x 세로 num2 부교를 만든다.  예)  raft 8 10\nplayer.onChat(\"raft\", function (num1, num2) {\n    let w = num1\n    let d = num2\n    if (w < 1) {\n        w = 1\n    }\n    if (d < 1) {\n        d = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < w; i++) {\n        for (let j = 0; j < d; j++) {\n            agent.teleport(world(baseX + i, 30, baseZ + j), WEST)\n            loops.pause(250)\n            agent.place(DOWN)\n            loops.pause(200)\n        }\n    }\n    player.say(\"부교 완성\")\n})\n\n// [need] 부교에 블록이 몇 개 필요한지 미리 계산해서 보고한다.  예)  need 8 10\nplayer.onChat(\"need\", function (num1, num2) {\n    let total = num1 * num2\n    player.say(\"필요 블록\")\n    loops.pause(300)\n    player.say(total)\n    if (total > 64) {\n        player.say(\"인벤토리 부족\")\n    } else {\n        player.say(\"설치 가능\")\n    }\n})\n\n// [frame] 테두리만 두른다. 안쪽은 비운다.  예)  frame 8 10\nplayer.onChat(\"frame\", function (num1, num2) {\n    let w = num1\n    let d = num2\n    if (w < 2) {\n        w = 2\n    }\n    if (d < 2) {\n        d = 2\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < w; i++) {\n        agent.teleport(world(baseX + i, 30, baseZ), WEST)\n        loops.pause(250)\n        agent.place(DOWN)\n        loops.pause(200)\n        agent.teleport(world(baseX + i, 30, baseZ + d - 1), WEST)\n        loops.pause(250)\n        agent.place(DOWN)\n        loops.pause(200)\n    }\n    for (let j = 0; j < d; j++) {\n        agent.teleport(world(baseX, 30, baseZ + j), WEST)\n        loops.pause(250)\n        agent.place(DOWN)\n        loops.pause(200)\n        agent.teleport(world(baseX + w - 1, 30, baseZ + j), WEST)\n        loops.pause(250)\n        agent.place(DOWN)\n        loops.pause(200)\n    }\n    player.say(\"테두리 완성\")\n})\n\n// [sig8] 8층 신호 지점으로 투입\nplayer.onChat(\"sig8\", function () {\n    agent.teleport(world(-6, 30, -10), SOUTH)\n    loops.pause(400)\n    player.say(\"8층 진입. 침수 확인\")\n})"},
  {"n": 9, "y": 26, "x": -25, "z": 7, "title": "값을 쌓아간다", "subtitle": "변수 누적", "learn": "sum = sum + x", "cmds": ["total", "down", "up", "deck9"], "slot": "슬롯 1 → 석재 64개", "story": "9층은 바닥이 통째로 내려앉았어.\n관제실은 두 가지를 요구해.\n\n하나, 여러 층을 순서대로 점검할 것.\n둘, 재료가 얼마나 드는지 미리 계산할 것.", "mission": ["6차시 drill 을 다시 실행한다. 5층부터 9층까지만 가려면? i 로는 안 된다", "total 을 만든다. 반복할 때마다 sum 에 쌓는다", "실행 전에 sum 추적표를 종이에 채운다", "total 4 6 을 실행하고 24 → 48 → 72 → 96 이 나오는지 확인", "down 을 만든다. 시작 층과 끝 층을 둘 다 받는다", "반복 횟수가 왜 끝 - 시작 + 1 인지 손가락으로 세어 확인한다", "up 을 만든다. scanFloor 를 1씩 줄인다. 반복 횟수 식도 뒤집어야 한다", "edge 로 붕괴 가장자리에 서서 deck9 로 신호 지점 둘레에 발판을 만든다"], "observe": ["total 4 6 — 1번째 sum / 2번째 / 3번째 / 4번째", "sum = sum + 24 를 한 문장으로 설명해봐", "down 5 9 는 왜 다섯 번 도는 거야?", "i 와 내가 만든 변수의 차이는?", "total 로 예측한 값 / deck9 로 실제 쓴 블록 수"], "stretch": ["sum = sum + side * 4 에서 x 4 는 무슨 뜻일까. 면을 채우려면 어떻게 바꿔야 할까", "층마다 크기가 다르다면 지금 코드로 계산할 수 있을까", "down 9 5 처럼 거꾸로 주면 어떻게 될까. 방어 장치가 막아줄까"], "tip": "sum = sum + 24 는 수학의 등호가 아니야. '오른쪽을 계산해서 왼쪽에 다시 넣는다'는 뜻이야.", "code": "let sum = 0\nlet scanFloor = 1\n\n// [total] num1 개 층에 한 변 num2 짜리 발판을 놓으면 블록이 총 몇 개 드는지 쌓아간다\n// 예)  total 4 6\nplayer.onChat(\"total\", function (num1, num2) {\n    let floors = num1\n    let side = num2\n    if (floors < 1) {\n        floors = 1\n    }\n    if (side < 2) {\n        side = 2\n    }\n    sum = 0\n    for (let i = 0; i < floors; i++) {\n        sum = sum + side * 4\n        player.say(sum)\n        loops.pause(500)\n    }\n    player.say(\"총 블록 수\")\n    loops.pause(300)\n    player.say(sum)\n})\n\n// [down] num1 층부터 num2 층까지 한 층씩 내려가며 좌표를 보고한다.  예)  down 1 9\nplayer.onChat(\"down\", function (num1, num2) {\n    scanFloor = num1\n    if (scanFloor < 1) {\n        scanFloor = 1\n    }\n    let last = num2\n    if (last < scanFloor) {\n        last = scanFloor\n    }\n    for (let i = 0; i < last - scanFloor + 1; i++) {\n        let y = 62 - scanFloor * 4\n        agent.teleport(world(-12, y, 8), WEST)\n        loops.pause(600)\n        player.say(scanFloor)\n        loops.pause(200)\n        player.say(y)\n        scanFloor = scanFloor + 1\n    }\n    player.say(\"하강 점검 완료\")\n})\n\n// [up] 아래에서 위로 올라오며 점검한다.  예)  up 9 1\nplayer.onChat(\"up\", function (num1, num2) {\n    scanFloor = num1\n    let last = num2\n    if (last > scanFloor) {\n        last = scanFloor\n    }\n    for (let i = 0; i < scanFloor - last + 1; i++) {\n        let y = 62 - scanFloor * 4\n        agent.teleport(world(-12, y, 8), WEST)\n        loops.pause(600)\n        player.say(scanFloor)\n        loops.pause(200)\n        player.say(y)\n        scanFloor = scanFloor - 1\n    }\n    player.say(\"상승 점검 완료\")\n})\n\n// [deck9] 9층 붕괴 구간에 한 변 num1 짜리 발판을 만든다.  예)  deck9 6\nplayer.onChat(\"deck9\", function (num1) {\n    let side = num1\n    if (side < 2) {\n        side = 2\n    }\n    agent.teleport(world(-28, 26, 4), EAST)\n    loops.pause(500)\n    agent.setSlot(1)\n    loops.pause(200)\n    sum = 0\n    for (let turn = 0; turn < 4; turn++) {\n        for (let i = 0; i < side; i++) {\n            agent.place(DOWN)\n            loops.pause(250)\n            sum = sum + 1\n            agent.move(FORWARD, 1)\n            loops.pause(250)\n        }\n        agent.turn(RIGHT_TURN)\n        loops.pause(250)\n    }\n    player.say(\"사용한 블록\")\n    loops.pause(300)\n    player.say(sum)\n})\n\n// [sig9] 9층 신호 지점으로 투입. 붕괴 구멍 한가운데 남은 섬이다\nplayer.onChat(\"sig9\", function () {\n    agent.teleport(world(-25, 26, 7), EAST)\n    loops.pause(400)\n    player.say(\"9층 진입. 바닥 붕괴\")\n})\n\n// [edge] 발판을 놓기 시작할 붕괴 가장자리로 간다. deck9 는 여기서 시작한다\nplayer.onChat(\"edge\", function () {\n    agent.teleport(world(-28, 26, 4), EAST)\n    loops.pause(400)\n    player.say(\"붕괴 가장자리 도착\")\n})"},
  {"n": 10, "y": 22, "x": 0, "z": 22, "shot": true, "title": "거리를 계산한다", "subtitle": "두 좌표의 차", "learn": "부호가 방향이다", "cmds": ["dist", "roadx", "roadz", "tag"], "slot": "슬롯 1 → 석재, 슬롯 2 → 색 블록", "story": "10층은 갈림길이 많아. 갱도에서 신호 지점까지 길을 깔아야 해.\n\n몇 칸을 깔아야 할까. 눈대중으로 하면 모자라거나 남아.\n좌표를 알고 있는데 왜 세고 있지?", "mission": ["갱도(-12, 8)와 10층 신호(0, 22)의 차이를 종이에 계산한다", "마이너스를 빼면 더해진다는 걸 확인한다", "dist 를 만든다. 목표 - 현재 순서로 뺀다", "여러 층 신호 좌표를 넣어 표를 채우고 부호를 읽는다", "roadx 를 만든다. num2 가 1이면 step 이 1, 아니면 -1", "dx 가 12면 깔 칸은 13개다. 9차시에서 배운 끝-시작+1 이 또 나온다", "roadx 13 1 로 실행한다. 끝난 X를 roadEndX 가 기억한다", "방향을 반대로 넣어보고 왜 반대편에 깔리는지 설명한다", "roadz 를 만든다. 갱도가 아니라 roadEndX 에서 이어 깐다", "roadz 15 1 로 실행해 신호 지점 (0, 22) 까지 L자를 완성한다"], "observe": ["dist 0 22 → dx / dz / 각각 어느 쪽?", "dist -28 4 → dx / dz", "dist -18 -12 → dx / dz", "마이너스를 뺐는데 왜 더해졌어?", "i * step 이 방향을 어떻게 바꿔?", "차이가 12인데 왜 13칸을 깔아야 해?", "roadz 가 roadEndX 를 안 쓰면 길이 어떻게 될까?"], "stretch": ["dist 가 음수를 줄 때 방향값을 자동으로 정하게 하려면 뭐가 필요할까", "대각선으로 바로 가려면 roadx 와 roadz 를 어떻게 합쳐야 할까 (8차시 raft 힌트)", "갱도가 아니라 지금 에이전트 자리에서 거리를 재려면 뭐가 더 필요할까"], "tip": "목표 - 현재 순서로 빼. 순서를 바꾸면 부호가 뒤집혀서 반대로 가. 그리고 차이가 12면 칸은 13개야.", "code": "let shaftX = -12\nlet shaftZ = 8\n\n// roadx 가 끝난 자리를 기억한다. roadz 는 여기서 이어 깐다.\n// 이게 없으면 두 길이 갱도에서 각각 뻗어나가 L자가 안 된다\nlet roadEndX = -12\n\n// [dist] 갱도에서 목표 지점까지의 X차이, Z차이를 보고한다.  예)  dist 0 22\nplayer.onChat(\"dist\", function (num1, num2) {\n    let dx = num1 - shaftX\n    let dz = num2 - shaftZ\n    player.say(\"X 차이\")\n    loops.pause(300)\n    player.say(dx)\n    loops.pause(400)\n    player.say(\"Z 차이\")\n    loops.pause(300)\n    player.say(dz)\n})\n\n// [roadx] 갱도에서 X 방향으로 num1 칸 길을 깐다\n// num2 는 방향. 1이면 X가 커지는 쪽, 그 외는 작아지는 쪽\n// 예)  roadx 13 1\nplayer.onChat(\"roadx\", function (num1, num2) {\n    let len = num1\n    if (len < 1) {\n        len = 1\n    }\n    let step = -1\n    if (num2 == 1) {\n        step = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < len; i++) {\n        agent.teleport(world(shaftX + i * step, 22, shaftZ), WEST)\n        loops.pause(300)\n        agent.destroy(DOWN)\n        loops.pause(200)\n        agent.place(DOWN)\n        loops.pause(200)\n    }\n    roadEndX = shaftX + (len - 1) * step\n    player.say(\"가로 길 완성\")\n    loops.pause(300)\n    player.say(roadEndX)\n})\n\n// [roadz] roadx 가 끝난 자리에서 Z 방향으로 num1 칸 이어 깐다.  예)  roadz 15 1\n// roadx 를 먼저 실행해야 이어진다. 안 하면 갱도에서 곧장 뻗는다\nplayer.onChat(\"roadz\", function (num1, num2) {\n    let len = num1\n    if (len < 1) {\n        len = 1\n    }\n    let step = -1\n    if (num2 == 1) {\n        step = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < len; i++) {\n        agent.teleport(world(roadEndX, 22, shaftZ + i * step), WEST)\n        loops.pause(300)\n        agent.destroy(DOWN)\n        loops.pause(200)\n        agent.place(DOWN)\n        loops.pause(200)\n    }\n    player.say(\"세로 길 완성\")\n})\n\n// [tag] 갱도에서 X 방향으로 num1 개 표식을 num2 칸 간격으로 남긴다.  예)  tag 4 4\n// 층 홀은 X 2 까지다. 개수 x 간격이 그 밖으로 나가면 벽에 박힌다\nplayer.onChat(\"tag\", function (num1, num2) {\n    let count = num1\n    let gap = num2\n    if (count < 1) {\n        count = 1\n    }\n    if (gap < 1) {\n        gap = 1\n    }\n    agent.setSlot(2)\n    loops.pause(200)\n    for (let i = 0; i < count; i++) {\n        agent.teleport(world(shaftX + i * gap, 22, shaftZ), WEST)\n        loops.pause(300)\n        agent.destroy(DOWN)\n        loops.pause(200)\n        agent.place(DOWN)\n        loops.pause(200)\n    }\n    player.say(\"경로 표식 완료\")\n})\n\n// [sig10] 10층 신호 지점으로 투입\nplayer.onChat(\"sig10\", function () {\n    agent.teleport(world(0, 22, 22), WEST)\n    loops.pause(400)\n    player.say(\"10층 진입. 갈림길 다수\")\n})"},
  {"n": 11, "y": 18, "x": -18, "z": -12, "key": true, "title": "스스로 훑는다", "subtitle": "전부 합친다", "learn": "새 문법 없음. 여덟 가지를 한 명령에", "cmds": ["scan", "light", "relight", "patrol"], "slot": "슬롯 1 → 글로우스톤 64개", "story": "신호가 갑자기 강해졌어. 12층이 코앞이야.\n관제실은 마지막 점검을 지시해.\n\n1층부터 11층까지 전부 다시 훑고, 꺼진 유도등을 전부 다시 켤 것.\n\n층마다 명령을 만들면 스물두 개야. 지금까지 배운 걸 합치면 두 개면 돼.", "mission": ["지금까지 만든 명령 목록을 본다. 11층 유도등을 켜는 명령은 없다", "새로 만들지 말고 합칠 방법을 생각한다", "scan 을 만든다. 9차시 down 과 거의 같다", "light 를 만든다. 6차시 row 에서 층 좌표를 매개변수로 뺀다", "light 11 6 3 과 light 7 4 5 를 각각 실행한다", "relight 를 만든다. 바깥 반복이 층, 안쪽 반복이 유도등", "실행 전에 relight 6 11 의 총 설치 개수를 계산한다", "구조를 손가락으로 짚는다. y 는 언제 다시 계산되나, checkFloor 는 어디서 커지나"], "observe": ["scan 1 11 이 찍은 Y 열한 개를 순서대로", "relight 6 11 — 예상 설치 개수 / 실제", "바깥 반복은 뭘 세? 안쪽 반복은?", "y 는 언제 다시 계산돼?", "checkFloor 는 어디서 1 커져?", "이 명령 하나에 몇 차시 내용이 들어 있어?"], "stretch": ["relight 의 유도등 개수 3을 매개변수로 빼려면 어디를 고쳐야 할까", "층마다 유도등 간격을 다르게 하려면 뭐가 필요할까", "scan 에 free(7차시)를 넣어 층마다 열린 방향을 보고하게 만들어봐"], "tip": "새로 짜지 마. 이전 차시 코드를 찾아서 가져와. 그게 오늘의 진짜 실력이야.", "code": "let checkFloor = 1\nlet lampTotal = 0\n\n// [scan] num1 층부터 num2 층까지 순서대로 훑으며 좌표를 보고한다.  예)  scan 1 11\nplayer.onChat(\"scan\", function (num1, num2) {\n    checkFloor = num1\n    if (checkFloor < 1) {\n        checkFloor = 1\n    }\n    let last = num2\n    if (last < checkFloor) {\n        last = checkFloor\n    }\n    for (let i = 0; i < last - checkFloor + 1; i++) {\n        let y = 62 - checkFloor * 4\n        agent.teleport(world(-12, y, 8), WEST)\n        loops.pause(600)\n        player.say(y)\n        checkFloor = checkFloor + 1\n    }\n    player.say(\"전층 스캔 완료\")\n})\n\n// [light] num1 층 갱도에서 X 방향으로 유도등 num2 개를 num3 칸 간격으로 심는다\n// 예)  light 11 6 3\nplayer.onChat(\"light\", function (num1, num2, num3) {\n    let y = 62 - num1 * 4\n    let count = num2\n    let gap = num3\n    if (count < 1) {\n        count = 1\n    }\n    if (gap < 1) {\n        gap = 1\n    }\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < count; i++) {\n        agent.teleport(world(-12 - i * gap, y, 8), WEST)\n        loops.pause(350)\n        agent.destroy(DOWN)\n        loops.pause(200)\n        agent.place(DOWN)\n        loops.pause(200)\n        lampTotal = lampTotal + 1\n    }\n    player.say(\"설치 누적\")\n    loops.pause(300)\n    player.say(lampTotal)\n})\n\n// [relight] num1 층부터 num2 층까지 모든 층에 유도등 세 개씩 심는다.  예)  relight 6 11\n// scan 과 light 를 하나로 합친 형태다\nplayer.onChat(\"relight\", function (num1, num2) {\n    checkFloor = num1\n    let last = num2\n    if (last < checkFloor) {\n        last = checkFloor\n    }\n    lampTotal = 0\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let f = 0; f < last - checkFloor + 1; f++) {\n        let y = 62 - checkFloor * 4\n        for (let i = 0; i < 3; i++) {\n            agent.teleport(world(-12 - i * 4, y, 8), WEST)\n            loops.pause(300)\n            agent.destroy(DOWN)\n            loops.pause(200)\n            agent.place(DOWN)\n            loops.pause(200)\n            lampTotal = lampTotal + 1\n        }\n        player.say(y)\n        checkFloor = checkFloor + 1\n    }\n    player.say(\"총 설치\")\n    loops.pause(300)\n    player.say(lampTotal)\n})\n\n// [patrol] num1 층 홀의 네 귀퉁이를 순찰하며 감지 결과를 보고한다.  예)  patrol 11\nplayer.onChat(\"patrol\", function (num1) {\n    let y = 62 - num1 * 4\n    for (let i = 0; i < 4; i++) {\n        if (i == 0) {\n            agent.teleport(world(-26, y, -10), WEST)\n        }\n        if (i == 1) {\n            agent.teleport(world(-2, y, -10), WEST)\n        }\n        if (i == 2) {\n            agent.teleport(world(-2, y, 20), WEST)\n        }\n        if (i == 3) {\n            agent.teleport(world(-26, y, 20), WEST)\n        }\n        loops.pause(600)\n        if (agent.detect(AgentDetection.Block, DOWN)) {\n            player.say(\"바닥 있음\")\n        } else {\n            player.say(\"바닥 없음\")\n        }\n    }\n    player.say(\"순찰 완료\")\n})\n\n// [sig11] 11층 신호 지점으로 투입\nplayer.onChat(\"sig11\", function () {\n    agent.teleport(world(-18, 18, -12), WEST)\n    loops.pause(400)\n    player.say(\"11층 진입. 신호 강함\")\n})"},
  {"n": 12, "y": 14, "x": -12, "z": 8, "shot": true, "title": "신호원", "subtitle": "통합 미션과 발표", "learn": "새 문법 없음. 전부 잇는다", "cmds": ["dive12", "mine"], "slot": "슬롯 1 → 석재, 슬롯 2 → 글로우스톤", "story": "마지막 신호가 왔어.\n\nX도 Z도 갱도와 똑같아.\n열두 주 동안 옆으로 옮겨다니며 찾았는데,\n신호는 계속 우리 발밑에 있었어.", "mission": ["1차시 갱도 입구 좌표와 오늘 신호 좌표를 나란히 적고 비교한다", "코드를 열기 전에 네 단계를 종이에 쓴다. 각 단계 옆에 몇 차시 것인지도", "dive12 를 조립한다. 새로 치지 말고 이전 차시에서 복사해 붙인다", "실행한다. 2~3분 걸린다", "3단계와 2단계 순서를 바꿔 실행해본다", "mine 템플릿에 나만의 루틴을 만든다. 층과 크기는 반드시 채팅으로 받는다", "1분 동안 발표한다"], "observe": ["1차시 갱도 좌표 / 12차시 신호 좌표 — 뭐가 같아?", "네 단계를 순서대로. 각각 몇 차시 것을 썼어?", "3단계와 2단계 순서를 바꾸면 어떻게 됐어?", "내 루틴 이름은? num1 과 num2 에 뭘 넣게 했어?", "그 숫자를 바꾸면 뭐가 달라져?", "잘 안 된 건 뭐였어?", "열두 주 중에 제일 기억에 남는 층은? 왜?"], "stretch": ["12층에서 지상까지 되돌아 올라오는 루틴을 만들어봐", "relight 를 12층까지 늘리고 층마다 착륙장도 만들게 고쳐봐", "내가 만들고 싶은 13층을 설계해봐. 좌표, 상황, 필요한 명령을 종이에"], "tip": "완성 못 해도 괜찮아. 뭘 바꿨고 결과가 어떻게 달라졌는지 기록하면 그게 성공이야.", "ending": "12층 안쪽에 오래된 에이전트 한 대가 서 있다.\n30년 전 이 시설이 봉인될 때 혼자 남겨진 최초의 에이전트다.\n\n그때는 명령을 코드에 하나하나 박아 넣어야 했다.\n좌표도, 크기도, 횟수도 전부 손으로 고쳐야 했다.\n\n신호는 좌표를 보내며 계속 이렇게 말하고 있었다.\n\n\"여기 있어.\"", "code": "let missionFloor = 12\nlet deckW = 5\nlet deckD = 5\nlet hit = 0\n\n// [dive12] 12층 진입 통합 루틴.  예)  dive12\nplayer.onChat(\"dive12\", function () {\n    // 1단계 — 층 계산 투입\n    let y = 62 - missionFloor * 4\n    agent.teleport(world(-12, y, 8), WEST)\n    loops.pause(600)\n    player.say(y)\n\n    // 2단계 — 막힌 곳을 세며 통로 확보\n    hit = 0\n    for (let i = 0; i < 10; i++) {\n        if (agent.detect(AgentDetection.Block, FORWARD)) {\n            hit = hit + 1\n            agent.destroy(FORWARD)\n            loops.pause(250)\n            agent.collectAll()\n            loops.pause(250)\n        }\n        agent.move(FORWARD, 1)\n        loops.pause(250)\n    }\n    player.say(\"막힌 곳\")\n    loops.pause(300)\n    player.say(hit)\n\n    // 3단계 — 착륙장 채우기\n    agent.setSlot(1)\n    loops.pause(200)\n    for (let i = 0; i < deckW; i++) {\n        for (let j = 0; j < deckD; j++) {\n            agent.teleport(world(-24 + i, y, 4 + j), WEST)\n            loops.pause(250)\n            agent.destroy(DOWN)\n            loops.pause(200)\n            agent.place(DOWN)\n            loops.pause(200)\n        }\n    }\n    player.say(\"착륙장 완성\")\n\n    // 4단계 — 유도등 설치\n    agent.setSlot(2)\n    loops.pause(200)\n    for (let i = 0; i < 4; i++) {\n        agent.teleport(world(-22 - i * 2, y, 8), WEST)\n        loops.pause(300)\n        agent.destroy(DOWN)\n        loops.pause(200)\n        agent.place(DOWN)\n        loops.pause(200)\n    }\n    player.say(\"신호원 도달\")\n})\n\n// ============================================================\n// 나만의 관제 루틴 — 여기부터 직접 만든다\n//\n// 규칙 세 가지\n//   1. 층과 크기는 채팅으로 받는다. 코드에 숫자를 박지 않는다\n//   2. 좌표는 계산으로 만든다. 손으로 세지 않는다\n//   3. 완성 못 해도 된다. 무엇을 바꿨고 결과가 어떻게 달라졌는지 기록하면 성공이다\n//\n// 쓸 수 있는 재료\n//   agent.teleport(world(x, y, z), WEST)\n//   agent.move / turn / place / destroy / collectAll / setSlot\n//   if (agent.detect(AgentDetection.Block, FORWARD)) { } else { }\n//   for (let i = 0; i < n; i++) { }\n//   let y = 62 - 층 * 4\n//   변수 = 변수 + 1\n// ============================================================\n\nlet myCount = 0\n\n// [mine] 나만의 루틴.  예)  mine 9 6\n// num1 = 층,  num2 = 크기\nplayer.onChat(\"mine\", function (num1, num2) {\n    let y = 62 - num1 * 4\n    let size = num2\n    if (size < 1) {\n        size = 1\n    }\n    agent.teleport(world(-12, y, 8), WEST)\n    loops.pause(600)\n    player.say(\"루틴 시작\")\n    myCount = 0\n\n    // ↓ 여기에 직접 이어붙인다\n    // for (let i = 0; i < size; i++) {\n    //     agent.teleport(world(-12 - i, y, 8), WEST)\n    //     loops.pause(300)\n    //     agent.place(DOWN)\n    //     loops.pause(200)\n    //     myCount = myCount + 1\n    // }\n\n    player.say(\"루틴 종료\")\n    loops.pause(300)\n    player.say(myCount)\n})\n\n// [sig12] 12층 신호 지점으로 투입\nplayer.onChat(\"sig12\", function () {\n    agent.teleport(world(-12, 14, 8), WEST)\n    loops.pause(400)\n    player.say(\"12층 도착\")\n})"}
];
