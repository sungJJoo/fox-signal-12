// 웹 교재 설정 — 구글 시트 백업 주소를 여기 넣는다
//
// SHEET_URL 이 빈 문자열이면 시트 동기화 없이 브라우저에만 저장한다.
// 그 상태로도 수업은 정상적으로 돌아간다. 노트북이 바뀌면 기록이 안 따라올 뿐이다.
//
// 주소를 얻는 방법은 저장소의 sheet/README.md 참고.
// Apps Script 를 배포하면 나오는 /exec 로 끝나는 주소를 그대로 붙여넣는다.
//
// TOKEN 은 sheet/Code.gs 의 TOKEN 과 같은 값을 넣는다.
// 이 파일은 공개된 웹페이지라 토큰이 그대로 보인다. 진짜 보안이 아니라
// 주소를 우연히 알게 된 사람이 장난치는 걸 막는 정도의 장치다.

window.SIGNAL12_CONFIG = {
  SHEET_URL: "",
  TOKEN: ""
};
